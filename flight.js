export class Flight {
    constructor(origin, destination, ticketPrice, airline, durationInMinutes) {
        this.origin = origin
        this.destination = destination
        this.ticketPrice = ticketPrice
        this.airline = airline
        this.durationInMinutes = durationInMinutes
    }
}